@using FrontStore.Model

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Categories</h1>
            <table class="table table-bordered border-primary">
                <thead>
                    <tr>
                        <th>Category Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Categories)
                    {
                        <tr>
                            <td>@category.Name</td>
                            <td>
                                <i class="fas fa-edit" @onclick="() => EditCategory(category)"></i>
                                <i class="fas fa-trash" @onclick="() => DeleteCategory(category.Id)"></i>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@code {

    private List<Category> categories = new List<Category>();

    [Parameter]
    public EventCallback<Category> OnEditCategory { get; set; }

    [Parameter]
    public EventCallback<int> OnDeleteCategory { get; set; }

    [Parameter]
    public List<Category> Categories { get; set; }

    private void EditCategory(Category category)
    {
        OnEditCategory.InvokeAsync(category);
    }

    private void DeleteCategory(int categoryId)
    {
        OnDeleteCategory.InvokeAsync(categoryId);
    }


}
